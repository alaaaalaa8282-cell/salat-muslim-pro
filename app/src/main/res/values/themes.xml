<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- الثيم الرئيسي — يستخدم Material Components المضافة كـ dependency -->
    <style name="Theme.AlaApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
    </style>

    <!-- ثيم Splash Screen -->
    <style name="Theme.AlaApp.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.AlaApp</item>
    </style>

</resources>
